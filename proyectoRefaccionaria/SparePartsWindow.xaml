<winuiEx:WindowEx
    x:Class="proyectoRefaccionaria.SparePartsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winuiEx="using:WinUIEx"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls" 
    Title="Refaccionaria - Catálogo"
    Width="700" Height="500">

    <Grid Padding="20">
        <StackPanel Spacing="10">
            <TextBlock Text="Catálogo de Refacciones" 
                       FontSize="22"
                       FontWeight="Bold" />

            <ListView x:Name="PartsListView"
                      Height="150"
                      SelectionMode="Single"
                      DisplayMemberPath="Nombre"/>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <muxc:NumberBox x:Name="QuantityNumberBox"
                                Value="1"
                                Minimum="1"
                                SpinButtonPlacementMode="Compact"
                                SmallChange="1"
                                LargeChange="5"
                                Header="Cantidad"
                                Width="100"/>

                <Button Content="Agregar al carrito" 
                        Click="AddToCart_Click" 
                        Width="150"
                        VerticalAlignment="Bottom"/>
            </StackPanel>

            <TextBlock Text="Carrito de compra" 
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,20,0,0"/>

            <ListView x:Name="CartListView"
                      Height="150"
                      SelectionMode="Single"
                      DisplayMemberPath="DisplayName"/>
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                <Button Content="Quitar del carrito" Click="RemoveFromCart_Click" Width="150"/>
                <Button Content="Confirmar compra" Click="ConfirmPurchase_Click" Width="150"/>
                <Button Content="Cerrar sesión / Volver" 
                        Click="Logout_Click"
                        Width="200"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</winuiEx:WindowEx>
